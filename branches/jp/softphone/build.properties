# well-known directories
src.dir=src
lib.dir=lib
build.dir=build
build.lib.dir=${build.dir}/lib
classes.dir=${build.dir}/classes

# stun server
stun.dir=${top.dir}/stun
stun.lib.dir=${stun.dir}/lib
stun.build.lib.dir=${stun.dir}/build/lib

# common libraries
common.dir=${top.dir}/common
common.lib.dir=${common.dir}/lib
common.build.lib.dir=${common.dir}/build/lib

alsa.32bit.dir=audioframework/alsa32
alsa.64bit.dir=audioframework/alsa64
coreaudio.dir=audioframework/coreaudio

javadoc.dir=${build.dir}/javadoc
log.dir=log

source.version=1.5

# native libraries
native.lib.dir=${build.lib.dir}
alsa.native.32bit.library=${native.lib.dir}/libMediaFramework32.so
alsa.native.64bit.library=${native.lib.dir}/libMediaFramework64.so
coreaudio.native.library=${native.lib.dir}/libMediaFramework.jnilib

